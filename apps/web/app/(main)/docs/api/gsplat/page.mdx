import { pageMetadata } from "@/lib/page-metadata"
export const metadata = pageMetadata("docs/api/gsplat")

# @json-render/gsplat

Standalone Gaussian Splatting renderer for json-render. Load and display `.splat` and `.ply` files from JSON specs with built-in orbit controls and loading state management.

## Installation

```bash
npm install @json-render/gsplat @json-render/core react react-dom zod
```

## Entry Points

<table>
<thead>
<tr>
<th>Entry Point</th>
<th>Exports</th>
<th>Use For</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>@json-render/gsplat</code></td>
<td><code>gsplatComponents</code>, <code>GsplatRenderer</code>, <code>GaussianSplatViewerComponent</code>, schemas</td>
<td>React implementations and renderer</td>
</tr>
<tr>
<td><code>@json-render/gsplat/catalog</code></td>
<td><code>gsplatComponentDefinitions</code></td>
<td>Catalog schemas (safe for server)</td>
</tr>
</tbody>
</table>

## Usage

```tsx
import {'{ GaussianSplatViewerComponent }'} from "@json-render/gsplat";

<GaussianSplatViewerComponent
  props={'{'}
    width: "100%",
    height: "100vh",
    controls: true,
    autoRotate: true,
    cameraPosition: [0, 2, 5],
    cameraTarget: [0, 0, 0],
    fov: 50,
  {'}'}
  splats={'{['}
    {'{'}
      src: "https://huggingface.co/datasets/dylanebert/3dgs/resolve/main/bonsai/bonsai-7k.splat",
      position: [0, 0, 0],
    {'}'},
  {']}'}
/>
```

## Components

<table>
<thead>
<tr>
<th>Component</th>
<th>Description</th>
<th>Key Props</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>GaussianSplat</code></td>
<td>Loads a .splat/.ply file</td>
<td><code>src</code>, <code>position</code>, <code>rotation</code>, <code>scale</code>, <code>quality</code>, <code>alphaHash</code>, <code>toneMapped</code></td>
</tr>
<tr>
<td><code>GaussianSplatViewer</code></td>
<td>Canvas container with orbit controls and loading</td>
<td><code>width</code>, <code>height</code>, <code>controls</code>, <code>autoRotate</code>, <code>cameraPosition</code>, <code>cameraTarget</code>, <code>fov</code></td>
</tr>
</tbody>
</table>

### GaussianSplat Props

<table>
<thead>
<tr>
<th>Prop</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>src</code></td>
<td><code>string</code> (required)</td>
<td>URL to a <code>.splat</code> or <code>.ply</code> file</td>
</tr>
<tr>
<td><code>position</code></td>
<td><code>[x, y, z]</code></td>
<td>World position</td>
</tr>
<tr>
<td><code>rotation</code></td>
<td><code>[x, y, z]</code></td>
<td>Euler rotation</td>
</tr>
<tr>
<td><code>scale</code></td>
<td><code>[x, y, z]</code></td>
<td>Scale per axis</td>
</tr>
<tr>
<td><code>quality</code></td>
<td><code>"low" | "medium" | "high"</code></td>
<td>Render quality</td>
</tr>
<tr>
<td><code>alphaHash</code></td>
<td><code>boolean</code></td>
<td>Transparency mode</td>
</tr>
<tr>
<td><code>toneMapped</code></td>
<td><code>boolean</code></td>
<td>Tone mapping toggle</td>
</tr>
<tr>
<td><code>visible</code></td>
<td><code>boolean</code></td>
<td>Visibility toggle</td>
</tr>
</tbody>
</table>

### GaussianSplatViewer Props

<table>
<thead>
<tr>
<th>Prop</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>width</code></td>
<td><code>string</code></td>
<td>Canvas width (CSS)</td>
</tr>
<tr>
<td><code>height</code></td>
<td><code>string</code></td>
<td>Canvas height (CSS)</td>
</tr>
<tr>
<td><code>backgroundColor</code></td>
<td><code>string</code></td>
<td>Canvas background color</td>
</tr>
<tr>
<td><code>controls</code></td>
<td><code>boolean</code></td>
<td>Enable orbit controls</td>
</tr>
<tr>
<td><code>autoRotate</code></td>
<td><code>boolean</code></td>
<td>Auto-rotate camera</td>
</tr>
<tr>
<td><code>autoRotateSpeed</code></td>
<td><code>number</code></td>
<td>Rotation speed</td>
</tr>
<tr>
<td><code>cameraPosition</code></td>
<td><code>[x, y, z]</code></td>
<td>Initial camera position</td>
</tr>
<tr>
<td><code>cameraTarget</code></td>
<td><code>[x, y, z]</code></td>
<td>Camera look-at target</td>
</tr>
<tr>
<td><code>fov</code></td>
<td><code>number</code></td>
<td>Field of view</td>
</tr>
</tbody>
</table>

## vs. React Three Fiber GaussianSplat

This package provides a **standalone** viewer that doesn't require Three.js. If you need to compose gaussian splats with other 3D objects (meshes, lights, models), use the `GaussianSplat` component from `@json-render/react-three-fiber` instead — it integrates natively with the R3F scene graph via drei's Splat loader.

## Type Helpers

```tsx
import type {'{ GsplatProps }'} from "@json-render/gsplat";

type SplatProps = GsplatProps<"GaussianSplat">;
type ViewerProps = GsplatProps<"GaussianSplatViewer">;
```
