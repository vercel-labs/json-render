/**
 * @json-render/start/server — Server-safe exports
 *
 * This entry point does not import any React components or hooks.
 * Safe to use in build scripts, server handlers, and route config files.
 */

export {};
